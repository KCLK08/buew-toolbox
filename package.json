{
  "name": "baustellen-tools",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:SiteReport": "cd sitereport && npm run build",
    "build:Bautagebuch": "cd bautagebuch-v2 && npm run build",
    "deploy": "npm run build:SiteReport && npm run build:Bautagebuch && node scripts/deploy-toolbox.mjs"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
