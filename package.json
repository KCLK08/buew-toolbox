{
  "name": "baustellen-tools",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:SiteReport": "cd sitereport && npm run build",
    "build:Bautagebuch": "cd bautagebuch-v2 && npm run build",
    "deploy:bautagebuch": "npm run build:Bautagebuch && rm -rf /tmp/gh-pages-deploy && git clone --depth 1 --branch gh-pages https://github.com/KCLK08/buew-toolbox /tmp/gh-pages-deploy && rm -rf /tmp/gh-pages-deploy/bautagebuch && mkdir -p /tmp/gh-pages-deploy/bautagebuch && cp -R bautagebuch-v2/build/* /tmp/gh-pages-deploy/bautagebuch/ && cd /tmp/gh-pages-deploy && git add bautagebuch && git commit -m \"deploy(bautagebuch): update production build\" && git push origin gh-pages",
    "deploy": "npm run build:SiteReport && rm -rf dist && mkdir -p dist/sitereport && cp -R index.html tool-platzhalter-2 tool-platzhalter-3 dist/ && cp -R apple-touch-icon.png icon-192.png icon-512.png manifest.webmanifest dist/ && cp -R sitereport/build/* dist/sitereport/ && touch dist/.nojekyll && npx gh-pages -d dist --dotfiles --no-history"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
